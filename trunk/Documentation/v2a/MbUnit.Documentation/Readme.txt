This solution is for generating documentation for V2 of MbUnit.

The solution uses Sandcastle (blogs.msdn.com/sandcastle/) and DocProject (www.codeplex.com/DocProject).

Use build.bat to build the CHM and DocSite